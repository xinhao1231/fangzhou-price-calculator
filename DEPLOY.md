# 公网访问部署说明

这个价格计算器是静态网站，部署时只需要上传下面这些文件：

- `index.html`
- `styles.css`
- `app.js`
- `workspace.js`（产品搜索和样品单）
- `chatbot.js`（DeepSeek 产品问答）
- `chatbot.css`（聊天窗口样式）
- `chat-account.js`（账号登录和用户管理）
- `chat-config.js`（仅包含公开的后台网址，不能填写密钥）
- `chat-icons/`（聊天按钮图标，整个文件夹一起上传）
- `manifest.webmanifest`
- `service-worker.js`
- `seller-stamp.png`

更新网站时，请把压缩包解压后的文件上传到原仓库根目录并覆盖同名文件。不要只上传压缩包，也不要漏掉 `styles.css` 和 `workspace.js`。

样品单：选好产品后点击“加入样品单”，在“生成单据 → 样品单”里修改数量、样品单价、图片和快递费，再下载 Excel 或打印。样品单价默认带入当前优惠价（未优惠时为原单价），可以独立修改；样品单不自动加上整柜 FOB 或彩盒开版费。草稿自动保存在当前浏览器，也会随“导出价格数据”一起备份。

## 产品问答

以下步骤为个人密钥模式。共享账号需要额外部署账号后台，不能只上传 HTML 文件实现真正的密码保护。后台代码在项目的 `backend/`，详细步骤见 `backend/README.md`；不包含在公开网页压缩包里。

1. 上传更新文件后，点击顶部“方舟产品助手”或右下角“问问产品助手”。Mac 有独立会话侧栏，iPhone 使用全屏聊天。当前页面最多保留12个临时对话，刷新后清除。
2. 在“账号 / 设置”中选择“个人密钥”。在 DeepSeek 控制台 `https://platform.deepseek.com/api_keys` 创建自己的 API Key，输入并点击“应用设置”。请不要把密钥填写进网站代码或上传 GitHub。
3. 直接提问，例如“5L不锈钢脚踏，普通款，1000个彩盒，宁波40HQ的单品FOB美元价是多少？”或者“当前混装单的整柜和拼柜FOB各是多少？”

聊天直接从浏览器连接 DeepSeek 官方接口，无需额外服务器。API Key 和对话仅保留在当前页面内存中，刷新或关闭页面后清除；密钥不会包含在价格数据备份中。换设备需重新输入自己的密钥。发送问题会把提问、最近对话和所需产品/报价数据传给 DeepSeek，并产生该账户的 API 费用；不会自动发送 PI 买方信息、收款账号或产品图片。

助手从当前浏览器的产品目录、已保存价格和现有计算公式查询，缺失价格或外箱资料会提示待确认。它可以试算报价，但不会修改产品、混装单或生成/发送订单。当前没有外部实时运价查询功能。

### 图片识别报价

点击聊天输入框下方的“上传图片”，选择产品照片，也可以在输入框里粘贴截图。预览后点击“发送”；可以只发送一张图片，或同时写明容量、数量、材质和包装要求。每条消息可附一张图片，后续对话可继续补充其他图片。

有图片的当前对话会自动使用支持视觉输入的 DeepSeek Flash，继续使用同一个 API Key。助手根据可见外观和型号文字搜索网站产品目录，再读取对应价格；目录目前没有逐款参考图片，因此不能保证仅凭外观精确识别型号。容量、厚度、材质、缓降等看不清的配置会请求确认，候选产品报价应视为相应配置的参考价。

图片仅在点击发送后传给 DeepSeek，不上传到 GitHub，不写入本地价格备份。图片和聊天记录只保存在当前页面内存，刷新后清除。限制为每张12MB，发送前自动等比例缩小；支持 JPG、PNG、WebP、GIF，iPhone HEIC/HEIF 取决于浏览器能否读取，读取失败时可换成截图或 JPG。图片识别同样会消耗 API 额度。官方视觉接口说明：`https://api-docs.deepseek.com/guides/vision/`。

如果显示密钥无效，重新输入有效 Key；余额不足则在 DeepSeek 账户充值；无法连接则检查当前网络。默认模型为 `deepseek-flash`，也可以在设置里选择 `deepseek-v4-pro`。官方文档：`https://api-docs.deepseek.com/`。

推荐方式：

1. Netlify Drop：打开 `https://app.netlify.com/drop`，把整个文件夹拖进去，会得到一个公网网址。
2. Cloudflare Pages：新建 Pages 项目，上传这些静态文件，会得到一个公网网址。
3. GitHub Pages：把这些文件放到一个 GitHub 仓库，开启 Pages。

注意：

- 目前价格会保存在网站文件和浏览器本地数据里。换设备访问公网网站时，会读取网站内置价格。
- 后续如果你在浏览器里新增或修改价格，点“导出价格数据”可以备份；如果要让公网版本也更新，需要把新数据同步进网站文件后重新部署。
