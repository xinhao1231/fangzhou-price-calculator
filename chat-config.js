// Public settings only. Never put API keys or passwords here.
window.FANGZHOU_CHAT_CONFIG = Object.freeze({
  apiBase: location.hostname === "endearing-elf-697b4e.netlify.app"
    ? location.origin
    : "https://fangzhou-chat.xinhao-meta-leads.workers.dev"
});
